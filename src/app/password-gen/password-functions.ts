
type PasswordOptions = {
    numWords: number;
    capitalFirstChar: boolean;
    includeDigit: boolean;
    includeSpecial: boolean;
    minLength: number;
    maxLength: number;
}

function generatePassword(options: PasswordOptions) {
    
}
